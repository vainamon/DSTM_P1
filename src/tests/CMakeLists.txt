#FIND_PACKAGE (GTest REQUIRED)
#FIND_PACKAGE (Threads REQUIRED)

#INCLUDE_DIRECTORIES (${GTEST_INCLUDE_DIR})

#link_directories ()

#add_executable ()

#target_link_libraries (${GTEST_LIBRARY})


#SET(Args "")

#GTEST_ADD_TESTS( "${Args}" )

